Input:
Enter the file name: test2.txt

for i in range(1, 6):
if i == 3:
print('Found it!')
continue
print(i)

def factorial(n):
if n == 0:
return 1
else:
return n * factorial(n-1)

print(factorial(5))

lexems ---------- token
for ---------- keyword
i ---------- identifier
in ---------- keyword
range ---------- keyword
( ---------- separator
1 ---------- real
, ---------- comma
6 ---------- real
) ---------- separator
: ---------- colon
if ---------- keyword
i ---------- identifier
== ---------- operator
3 ---------- real
: ---------- colon
print ---------- keyword
( ---------- separator
'Found it!' ---------- identifier
) ---------- separator
continue ---------- keyword
print ---------- keyword
( ---------- separator
i ---------- identifier
) ---------- separator
def ---------- keyword
factorial ---------- identifier
( ---------- separator
n ---------- identifier
) ---------- separator
: ---------- colon
if ---------- keyword
n ---------- identifier
== ---------- operator
0 ---------- real
: ---------- colon
return ---------- keyword
1 ---------- real
else ---------- keyword
: ---------- colon
return ---------- keyword
n ---------- identifier
* ---------- operator
factorial ---------- identifier
( ---------- separator
n ---------- identifier
- ---------- operator
1 ---------- real
) ---------- separator
print ---------- keyword
( ---------- separator
factorial ---------- identifier
( ---------- separator
5 ---------- real
) ---------- separator
) ---------- separator
